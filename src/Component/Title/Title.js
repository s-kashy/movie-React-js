import React from "react";
import classes from "./Title.css"
const Title = props => {
  return (
    <div>
      <p className={classes.Title}>Movie List For Restart IT</p>
    </div>
  );
};

export default Title;
